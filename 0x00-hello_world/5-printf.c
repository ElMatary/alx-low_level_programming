#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Using the function printf in a C programe
 *
 * Return: always 0 (succes)
*/

main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
